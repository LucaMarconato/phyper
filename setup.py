#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='Phyper',
      version='0.1.0',
      description='Dealing with hyperparameters in machine learning pipelines',
      author='Luca Marconato',
      author_email='luca.marconato@embl.de',
      packages=find_packages(),
      )
